---
title: '前端应用结构设置'
createdAt: '2023-04-15'
publishedAt: '2023-04-15'
updatedAt: '2023-04-15'
draft: false
summary: '我在日常开发中如何处理魔法字符串、异常的捕获、模块的组织、错误边界的处理'
tags: ['指南']
components: ['Tree']
---

{/* https://www.arc.id.au/Spectrogram.html */}
{/* https://github.com/vlandham/spectrogramJS */}
{/* https://github.com/miguelmota/spectrogram */}
{/* https://vue3.chengpeiquan.com/ */}

## `/src`

现如今的前端工程，几乎所有的项目都会是类似这样的结构：

<Tree data={spa} />

我们将整个项目的业务代码在 `/src` 目录中书写，这很自然。

> 源码放到一个固定的目录中有什么好处？

我个人认为，是**利于构建脚本的编写**，不论是何种框架，还是何种构建工具，抽象来讲，都需要做同一件事情：

将 `input file` 构建后输出成 `output file`，即文件到文件的转换。

那么这些 `input files` 公

## 环境变量

## 魔法字符串

## 模块目录

假设，我们需要一个 `./constants/` 目录来放置整个项目的常量文件，其中包含了，按照不同常量的使用场景，有如下分类，例如：

- 环境变量: `env.js`
- 消息文案: `text.js`
- 发布订阅: `emitter.js`

<Tree data={structure} />

```js
export * from 'xxx'
```

export const spa = [
  {
    type: 'folder',
    name: 'src',
    expand: true,
    childrens: [
      {
        type: 'folder',
        name: 'components',
      },
      {
        type: 'folder',
        name: 'pages',
      },
      {
        type: 'folder',
        name: 'routes',
      },
      {
        type: 'file',
        name: 'main.js',
      },
    ],
  },
  {
    type: 'file',
    name: 'index.html',
  },
  {
    type: 'file',
    name: 'package.json',
  },
]

export const structure = [
  {
    type: 'folder',
    name: 'constants',
    expand: true,
    childrens: [
      {
        type: 'file',
        name: 'env.js',
      },
      {
        type: 'file',
        name: 'text.js',
      },
      {
        type: 'file',
        name: 'emitter.js',
      },
      {
        type: 'file',
        name: 'index.js',
      },
    ],
  },
]
